import { createApp } from 'vue'
import App from './App.vue'
import router from './router'

createApp(App).use(router).mount('#app')

/**===================================================
 * configurar el archivo vue.config.js, para un deploy personalizado
 * 
 * deploy en netfily
 * 
 * 
 * 
 *
 ===================================================*/